# 路线图（Roadmap）

> **⚠️ 非承诺**：个人项目，节奏随真实用户反馈调整。
> 组织方式：按 **结果（outcome）** 而不是功能清单；**不写超过 6 周的具体日期**（那多半是虚构）。
> 版本与里程碑的对应关系见 [`versioning.md`](versioning.md)，变更历史见 [`../CHANGELOG.md`](../CHANGELOG.md)。
> R1 的任务拆解与验收判据详见私有规划（`planning/19`），这里只放对外可讲的部分。

## 这个项目是什么

面向**在法国学习的中国学生**的本地课程资料问答助手：把法语课程资料（PDF / DOCX / PPTX / 图片）
解析成统一结构，再基于它们回答问题，**答案带可点击的页码引用**。
资料与向量都在本机，**不上传云端**；指标与成本账全部公开（见下方"已发布"）。

## 现在（Now · v0.2.0 = R1）

**R1 的目标不是加功能，而是让它"可以交给别人用"** —— 在干净环境跑得起来、用得下去
（会话 / 工作区 / 流式 / 失败能恢复）、升级不会弄坏自己的资料库，
并且保留**一条别人没有的能力**。

| 结果（Outcome） | 证据 | 状态 |
|---|---|---|
| 新用户能在 5 分钟内从上传走到**带引用的回答**（干净环境） | 竞品全部开箱即用；初筛被拒约 70% 死于跑不起来 | 进行中 |
| 使用者能**连开多个会话、按课程分开找东西**，不必清浏览器存储 | 现状：开新会话要清 `localStorage`；同类产品有 workspace 隔离 | 进行中 |
| 回答**逐字出现**（首字延迟可测），而不是整段等待 | 竞品全有；我们目前只有端到端 P50 5.6 s，没有首字数据 | 进行中 |
| 小语种课程里的**术语能被自测**（唯一差异化） | 中法术语对照：同类产品没有；头部产品的闪卡不做跨语言 | 进行中 |
| 升级到新版本**不会弄坏已有资料库** | 同类产品都有数据迁移机制；我们目前只有一个空目录 | 进行中 |

## 接下来（Next）

| 结果（Outcome） | 证据 | 状态 |
|---|---|---|
| 按课程收窄检索范围后，找东西更快 | **待补证据**：需先验证"混合索引是否真的更差"（当前全量索引 @5 92.3%） | 已成形，待证据 |
| 扫描件 / PPTX 资料能被直接使用 | 头部产品做不到（PPTX 不能直传、扫描件无文字层则读不了） | 待验证 |

## 以后（Later）

| 方向 | 状态 |
|---|---|
| 术语卡 / 错题重做（把"自测"变成"复习"） | 想法，未成形 |
| 图片**内容**理解（不只是 OCR 文字） | 想法，未成形 |
| 文档批量管理（删除 / 去重 / 归档） | 想法，未成形 |

> 规则：`Later` 里的项只有在拿到证据后才升到 `Next`；每季度清理一次（没有证据的删掉）。

## 已发布

### v0.1.0 —— 工程 MVP（2026-09-14）

上传 → 解析 → 落库 → 切块 → 向量化 → 检索 → **带来源回答**，界面接真实数据。

| 能力 | 状态 |
|---|---|
| 多格式解析（PDF / DOCX / PPTX / 图片 OCR）、异步上传 + 进度 | ✅ |
| 本地 embedding（`multilingual-e5-large`）+ sqlite-vec 检索 | ✅ |
| 带引用回答；**点引用跳到原文页**；正文内 `[来源 N]` 可点 | ✅ |
| Agent 自主调用检索工具（有界循环 + 失败降级直答） | ✅ |
| 六个工具按 **MCP 协议**暴露（stdio） | ✅ |
| 会话与消息落库、刷新后恢复；模型可在界面配置 | ✅ |
| 结构化 trace（`gen_ai.*` 对齐 OTel） | ✅ |

**可复现的公开指标**（语料随仓库分发，clone 后能自己跑出来）：

| 层 | 指标 |
|---|---|
| L1 检索（合成语料 30 条） | HitRate@1 `76.9%` / @5 `92.3%` / @10 `100%` / MRR `0.850` |
| L2 生成（合成语料 30 条） | 答案带引用 `26/26`、引用召回 `93.5%`、文档命中 `92.5%` |
| 成本与延迟 | 单次提问 **≈ ¥0.01**；端到端 P50 `5.6 s`（首字延迟尚未测，无流式） |

> 私人课件的评测（46 条）不做公开分发 —— 它有著作权；那份报告的**方法**与代码是公开的。

## 明确不做（沿用既有决定，欢迎质疑）

| 不做 | 原因 |
|---|---|
| 多用户 / 认证 / 协作分享 | 单人单机是当前形态；要先有真实用户 |
| 章节树切块 / reranker / 混合检索 | 需先用评测证明收益 > 10pp，否则是凭感觉加复杂度 |
| Tauri 桌面端 | 维护成本（打包、三平台）远高于它带来的价值 |
| 云端部署 / 规模化 | 与"资料不出本机"的定位冲突 |

## 怎么跟进

- **变更历史**：[`CHANGELOG.md`](../CHANGELOG.md)（每个版本一个 tag、一个 Release）
- **指标与账本**：[`eval-report-synthetic.md`](eval-report-synthetic.md)、[`eval-report-l2-synthetic.md`](eval-report-l2-synthetic.md)、[`cost-and-latency.md`](cost-and-latency.md)
- **工程细节**：[`specs/`](specs/) 下的各批 US 规格；[`../AGENTS.md`](../AGENTS.md) 是给协作者 / agent 的项目说明
- **版本管理方案**：[`versioning.md`](versioning.md)
